# Memory Log - 2026-04-30

## GB Fantasy App - Session Summary

### Logout Functionality Fix
- Fixed logout button across all pages in GB Fantasy app
- Added auth-gate.js to all HTML pages (index, injuries, links, login, predictions, profile, register, table, tournaments, admin, leaderboard)
- Navbar now shows "Logout" when logged in, "Login/Register" when logged out
- Sidebar footer shows user name + logout button when authenticated
- Changes pushed to GitHub and deployed to Vercel

### Data Verification - GW35
- Confirmed 3 users in system: Angela, Jess, Mark G
- 2 tournaments created: "GW35 T22222" and "GW35 T1"
- 4 tournament entries (Mark G in 2 tournaments, others in 1 each)
- 30 prediction records for GW35
- All data correctly saved in Supabase

### Technical Notes
- RLS policies prevent anon key from reading all data (by design)
- Need service role key for full admin queries
- Vercel deployment auto-updates on git push

## 2026-04-30 Session Summary

### Tournament Points Fix
- **Problem:** Profile page showing 0 points for tournament entries even though users had earned points
- **Root Cause:** `tournament_entries.entry_points` column was never being populated when match scores were updated
- **Fixes Applied:**
  1. Updated `api/live-scores.js` - Added `updateTournamentEntries()` function to update entry_points when matches finish
  2. Updated `api/sync-fixtures.js` - Added tournament entry updates when syncing fixtures with results
  3. Updated `api/gameweek-transition.js` - `updateTournamentRankings()` now populates entry_points from gameweek_summary
  4. Updated `api/tournaments.js` - Added `my_entries=true` query parameter to fetch user's tournament entries with points
  5. Updated `frontend/profile.js` - Changed `loadMyTournaments()` to use the new endpoint and display entry_points

### SQL Fix for Existing Data
- Ran SQL to manually populate `entry_points` for existing tournament entries:
```sql
UPDATE tournament_entries te
SET entry_points = COALESCE(
  (SELECT SUM(p.points_earned)
   FROM predictions p
   WHERE p.user_id = te.user_id
   AND p.gameweek = (SELECT gameweek FROM tournaments t WHERE t.id = te.tournament_id)
  ), 0
);
```
- Result: Jess now shows 20 points in GW35 T1 (correct), Mark G and angela show 0 (correct - they predicted incorrectly)

### Outstanding Issue
- Manual Score Entry UI exists on Vercel deployment but not in GitHub repository
- Need to verify this UI's API endpoint updates tournament entries
- User checking Vercel Source tab to find the code

### Next Steps
- Once manual score entry code is found, verify it calls an API that updates tournament_entries
- Both manual and auto workflows should now work correctly for fresh data
